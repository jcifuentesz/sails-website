<h1 id="remove-from-collection">Remove from Collection</h1>
<p>Removes a record from an associated collection between two records.</p>
<pre><code data-language="javascript">DELETE /:model/:id/:associatedCollection/:recordIdToRemove
</code></pre><!-- __LANG=%%__ -->
<p>This action removes a reference to some other record (the &quot;foreign&quot; record) from a collection attribute of this record (the &quot;primary&quot; record).</p>
<ul>
<li>If the foreign record does not exist, it is created first.</li>
<li>If the collection doesn&apos;t contain a reference to the foreign record, this action will be ignored.</li>
<li>If the association is 2-way (i.e. reflexive, with &quot;via&quot; on both sides) the association on the foreign record will also be updated.</li>
</ul>
<h3 id="parameters" permalink="parameters">Parameters</h3>
<table>
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th style="text-align:left">Details</th>
</tr>
</thead>
<tbody>
<tr>
<td> id<br><em>(required)</em></td>
<td><bubble type="number" colors="true"></bubble><br><em>-or-</em><br><bubble type="string" colors="true"></bubble></td>
<td style="text-align:left">The desired record&apos;s primary key value<br><br>e.g. <code>/store/16</code></td>
</tr>
<tr>
<td> associatedCollection<br><em>(required)</em></td>
<td><bubble type="string" colors="true"></bubble></td>
<td style="text-align:left">The name of the collection of associated models<br><br>e.g. <code>/emplployeesOfTheMonth</code></td>
</tr>
<tr>
<td> recordIdToRemove<br><em>(required)</em></td>
<td><bubble type="number" colors="true"></bubble><br><em>-or-</em><br><bubble type="string" colors="true"></bubble></td>
<td style="text-align:left">The id of the record to remove from the collection<br><br>e.g. <code>7</code> (The Dolly employee model id)</td>
</tr>
<tr>
<td> callback</td>
<td><bubble type="string" colors="true"></bubble></td>
<td style="text-align:left">If specified, a JSONP response will be sent (instead of JSON). This is the name of the client-side javascript function to call, passing results as the first (and only) argument<br> <br> e.g. <code>?callback=myJSONPHandlerFn</code></td>
</tr>
</tbody>
</table>
<h3 id="example" permalink="example">Example</h3>
<p>Remove Dolly (employee #7) from the <code>employeesOfTheMonth</code> list of store #16, E.g. <code>http://localhost:1337/store/16/employeesOfTheMonth/7</code></p>
<h4 id="route" permalink="route">Route</h4>
<p><code>DELETE /store/16/employeesOfTheMonth/7</code></p>
<h4 id="expected-response" permalink="expected-response">Expected Response</h4>
<pre><code data-language="json">{
  &quot;employeesOfTheMonth&quot;: [],
  &quot;name&quot;: &quot;Dolly&quot;,
  &quot;createdAt&quot;: &quot;2014-08-03T01:16:35.440Z&quot;,
  &quot;updatedAt&quot;: &quot;2014-08-03T01:51:41.567Z&quot;,
  &quot;id&quot;: 16
}
</code></pre><!-- __LANG=%%__ -->
<h3 id="full-example" permalink="full-example">Full Example</h3>
<p>The example above assumes &quot;rest&quot; blueprints are enabled, and that your project contains at least an empty &apos;Employee&apos; model as well as a <code>Store</code> model with association: <code>employeesOfTheMonth: {collection: &apos;Employee&apos;}</code>.  You&apos;ll also need at least an empty <code>StoreController</code> and <code>EmployeeController</code>.  You can quickly achieve this by running:</p>
<pre><code data-language="shell">$ sails new foo
$ cd foo
$ sails generate api store
$ sails generate api employee
</code></pre><!-- __LANG=%%__ -->
<p>...then editing <code>api/models/Store.js</code>.</p>
<pre><code data-language="javascript">module.exports = {
 attributes: {
   employeesOfTheMonth: {collection: &apos;Employee&apos;}
 }
}
</code></pre><!-- __LANG=%%__ -->
<p>You will then need to create a store with an employeesOfTheMonth collection as described <a href="https://github.com/jodonnell-broadsoft/sails-docs/blob/master/reference/blueprint-api/Create.md#create-new-record-while-associating-w-another-new-record-rest" target="_blank">here</a></p>
<p>With a POST payload of:</p>
<pre><code data-language="javascript">{
    &quot;name&quot;: &quot;Starbucks&quot;,
  &quot;id&quot;: 16,
    &quot;employeesOfTheMonth&quot;: [{
        &quot;name&quot;: &quot;Dolly&quot;
    }]
}
</code></pre><!-- __LANG=%%__ -->
<p>To find Dolly&apos;s id: <code>http://localhost:1337/store/16</code>
<!-- __LANG=%%__ --></p>
<p>Then Remove her from the collection: <code>DELETE /store/16/employeesOfTheMonth/7</code></p>
<h3 id="notes" permalink="notes">Notes</h3>
<blockquote>
<ul>
<li>This action is for dealing with <em>plural</em> (&quot;collection&quot;) associations.  If you want to set or unset a <em>singular</em> (&quot;model&quot;) association, just use <a href="http://sailsjs.org/documentation/reference/blueprint-api/Update.html" target="_blank">update</a>.</li>
</ul>
</blockquote>
<docmeta name="displayName" value="remove from">
</docmeta>